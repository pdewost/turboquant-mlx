# TurboQuant Mac — оперативная память проекта

## Стек
- Python 3
- Apple MLX (для ускорения на Metal)
- NumPy / SciPy (для прототипирования математики)

## Запуск
```bash
# Будет добавлено позже, после создания прототипа
```

## Архитектура
Цель: реализовать алгоритмы QJL (Quantized Johnson-Lindenstrauss) и PolarQuant для квантования чисел до 1-3 бит без существенной потери точности (TurboQuant оборачивает их обоих).
- `core/qjl.py` — реализация Quantized Johnson-Lindenstrauss
- `core/polarquant.py` — реализация PolarQuant (координатные трансформации)
- `core/turboquant.py` — обертка, оркеструющая оба алгоритма
- `tests/` — валидация точности на рандомных тензорах

## Ключевые решения
- Сначала пишем чистый прототип на NumPy / PyTorch, чтобы убедиться в правильности математики.
- Затем портируем на `mlx-core` (Apple MLX), чтобы задействовать GPU M4.
- Ограничение: никаких интеграций с LLM до подтверждения, что тензоры жмутся/разжимаются с приемлемым искажением dot-product.

## Известные проблемы / Tech Debt
- Текущие проблемы отсутствуют. В дальнейшем: проверить оптимальное число случайных матриц в QJL.

## Что делали последним (2026-03-25)
- Проект упакован в полноценный Python/pip пакет (`pyproject.toml`).
- Написана интеграционная обертка `apply_turboquant_cache` (`mlx_core/cache.py`) для бесшовной подмены кэша на лету в моделях `mlx-lm`.
- Оформлен красивый и понятный `README.md` с бенчмарками и инструкциями.

## Следующие задачи
1. Реализовать механизм батч-квантования (chunking) внутри метода `update_and_fetch` (в `mlx_core/cache.py`), чтобы кэшировать токены динамически по мере генерации текста.
2. Протестировать с реальной LLM на `mlx-lm` (Llama 3.1 8B или Gemma 2) генерацию "Needle in a Haystack" с 64K+ токенами контекста.
